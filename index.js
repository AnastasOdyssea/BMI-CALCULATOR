const age = getElementById('age') ;
const height = getElementById('height');
const weight = getElementById('weight');
console.log("age");

function result() {
    var bmi = 0;
    bmi = height/(Math.pow(height,2));
}
